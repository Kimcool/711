
{
  "name": "711-finder",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && npx esbuild index.tsx --bundle --outfile=dist/index.js --format=esm --jsx=automatic --define:process.env.API_KEY=\"\\\"$API_KEY\\\"\" --external:react --external:react-dom --external:@google/genai && cp index.html metadata.json dist/ && sed -i 's/src=\".\\/index.tsx\"/src=\".\\/index.js\"/g' dist/index.html"
  },
  "dependencies": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "@google/genai": "^1.34.0",
    "esbuild": "^0.24.0"
  }
}
